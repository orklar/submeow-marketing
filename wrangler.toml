name = "submeow-marketing"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages configuration
# Deploy with: wrangler pages deploy
# Or use: npm run deploy (includes build step)
